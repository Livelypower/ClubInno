{% extends 'base.html.twig' %}

{% block title %}New Activity{% endblock %}

{% block body %}
    {{ parent() }}
    <div class="row">
        <div class="col s12">
            {{ form(form) }}
            <a class="btn" href="{{ path('activity') }}">Back</a>
        </div>
    </div>
{% endblock %}