{% extends 'base.html.twig' %}

{% block title %}Mail envoyé{% endblock %}

{% block body %}
    {{ parent() }}
    <div class="row">
        <div class="col m3"></div>
        <div class="col s12 m6">
            <div class="card blue-grey darken-1">
                <div class="card-content">
                    <span class="card-title white-text">Mail envoyé</span>
                    <div class="row">
                        <p>
                            Un mail avec un nouveau mot de passe est envoyé!
                        </p>
                    </div>
                </div>
                <div class="card-action">
                    <a href="{{ path('home') }}" class="btn red accent-4">Page d'acceuil</a>
                </div>
            </div>
        </div>
    </div>
{% endblock %}

{% block javascripts %}
    {{ parent() }}
{% endblock %}